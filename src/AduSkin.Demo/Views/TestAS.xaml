<UserControl x:Class="AduSkin.Demo.Views.TestAS"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:AduSkin="clr-namespace:AduSkin.Controls.Metro;assembly=AduSkin"
            xmlns:AduOpenLink="clr-namespace:AduSkin.Interactivity;assembly=AduSkin"
            xmlns:AduAttach="clr-namespace:AduSkin.Controls.Attach;assembly=AduSkin"
            xmlns:viewmodel="clr-namespace:AduSkin.Demo.ViewModel">
   <UserControl.DataContext>
      <viewmodel:TestASViewModel/>
   </UserControl.DataContext>
   <UserControl.Resources>
      <Style TargetType="TextBlock">
         <Setter Property="HorizontalAlignment" Value="Center"/>
         <Setter Property="Foreground" Value="{DynamicResource NormalForeground}"/>
      </Style>
   </UserControl.Resources>
   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="Auto"/>
         <RowDefinition/>
      </Grid.RowDefinitions>
      <StackPanel  VerticalAlignment="Center" Margin="0,10">
         <TextBlock Style="{StaticResource H2}" Text="OpenCV ROI 轮廓提取 Demo" Foreground="{DynamicResource NormalForeground}" FontWeight="Bold" Margin="0,20"/>
         <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="加载图片" Command="{Binding LoadImageCommand}" Margin="5" Padding="15,8"/>
            <Button Content="清除轮廓" Command="{Binding ClearContoursCommand}" Margin="5" Padding="15,8"/>
            <Button Content="提取轮廓" Command="{Binding ExtractContoursCommand}" Margin="5" Padding="15,8"/>
         </StackPanel>

         <TextBlock Text="{Binding StatusMessage}" Margin="0,5" Foreground="{DynamicResource NormalBrush}" FontSize="14"/>
      </StackPanel>

      <!-- ── 第二行：图像 + 叠加层 ─────────────────────────── -->
      <Grid Grid.Row="1" Margin="10">

         <!-- 原图 / 结果图 -->
         <Image x:Name="MainImage"
                Source="{Binding DisplayImageSource}"
                Stretch="Uniform"
                MouseLeftButtonDown="Image_MouseLeftButtonDown"
                MouseMove="Image_MouseMove"
                MouseLeftButtonUp="Image_MouseLeftButtonUp"
                Cursor="Cross"/>

         <!-- ROI 选择矩形（拖拽时显示） -->
         <Canvas x:Name="OverlayCanvas" IsHitTestVisible="False">
            <Rectangle Canvas.Left="{Binding RectX}"
                       Canvas.Top="{Binding RectY}"
                       Width="{Binding RectWidth}"
                       Height="{Binding RectHeight}"
                       Stroke="{DynamicResource NormalBrush}"
                       StrokeThickness="2"
                       StrokeDashArray="4,2"
                       Fill="{DynamicResource NormalBrush}"
                       Opacity="0.3"
                       Visibility="{Binding IsSelecting,
                                    Converter={StaticResource BoolToVisibilityConverter}}"/>
         </Canvas>

         <!-- 轮廓叠加层 -->
         <Canvas x:Name="ContourCanvas" IsHitTestVisible="False">
            <Path Data="{Binding ContourPaths}"
                  Stroke="{DynamicResource AccentBrush}"
                  StrokeThickness="2"
                  Fill="Transparent"
                  Stretch="None"/>
         </Canvas>

      </Grid>
   </Grid>
</UserControl>
